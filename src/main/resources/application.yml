server:
  port: 3031
  servlet:
    context-path: /hstl
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Reset123
    url: jdbc:mysql://localhost:3306/finaldb2
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57InnoDBDialect
        format_sql: true
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
